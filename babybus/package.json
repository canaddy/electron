{
  "name": "Helloworld",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",  
  "author": "babybus",
  "license": "ISC",
  "devDependencies": {
    "electron-packager": "^8.1.0",
    "electron-prebuilt": "^1.4.3"
  },

  "scripts": {
    "start": "electron .",
    "packager": "electron-packager ./ HelloWorld --platform=win32 --arch=x64 --out ../OutApp --version 1.4.3 --icon=./2.ico --ignore=dev-settings --ignore=node_modules/electron-* --ignore=node_modules/.bin --ignore=node_modules/electron-packager --ignore=node_modules/electron-prebuilt --prune"
  }
}
