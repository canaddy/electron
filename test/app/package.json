{
  "name": "feedback",
  "version": "0.0.1",
  "description": "全线升级每日反馈",
  "main": "main.js",
  "scripts": {
     "start": "electron .",
     "package": "electron-packager ./ feedback --platform=darwin --arch=x64 --overwrite --out ../OutApp --version 1.3.8 --asar",
      "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "canaddy",
  "license": "ISC",
  "dependencies": {
    "electron-builder": "^7.15.2",
    "electron-packager": "^8.1.0",
    "electron-prebuilt": "^1.3.8"
  }
}
